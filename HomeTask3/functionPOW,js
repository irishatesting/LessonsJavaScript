let number = 2;
let powerOfNum = 5;

function pow () {
    let result = number;
    for (let i = 1; i < powerOfNum; i++) {
         result = result * number;
    }
    return result; 
};

console.log(pow(number, powerOfNum));
